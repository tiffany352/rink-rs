name: Build Release Binaries

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ${{ matrix.config.os }}
    strategy:
      matrix:
        config:
          - os: ubuntu-latest
            asset-name: rink
          - os: macos-latest
            asset-name: rink
          - os: windows-latest
            asset-name: rink.exe

    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: cargo build --release --verbose
      - name: Upload Binary
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.config.asset-name }}
          path: target/release/${{ matrix.config.asset-name }}
