name: Build Release Binaries

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ${{ matrix.config.os }}
    strategy:
      matrix:
        config:
          - os: ubuntu-latest
            input-name: rink
            output-name: rink-linux
          - os: macos-latest
            input-name: rink
            output-name: rink-mac
          - os: windows-latest
            input-name: rink.exe
            output-name: rink.exe

    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: cargo build --release --verbose
      - name: Upload Binary
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.config.output-name }}
          path: target/release/${{ matrix.config.input-name }}
